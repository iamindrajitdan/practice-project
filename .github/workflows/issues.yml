name: Issues
on: [issues,workflow_dispatch]
jobs:
    output:
        runs-on: ubuntu-latest
        steps:
            - name: event details
              run: echo "${{ toJSON(github.event) }}"
    